    //演示程序dp23
    #include  <stdio.h>
    int  main(  )
    {
        int  varx, vary;                //定义变量
        int  varz;
        ;
        printf("input:varx,vary:");
        scanf("%d,%d", &varx, &vary);   //输入
        ;
        varz = varx + vary;             //计算表达式
        ;
        printf("varz=%d\n", varz);      //输出
        return  0;
    }
